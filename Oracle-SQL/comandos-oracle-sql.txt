MYSQL:
LIMIT and OFFSET:
-- 'LIMIT' reduzirá o número de linhas a serem retornadas e o 'OFFSET' opcional especificará 
	de onde começar a contar o número de linhas.

	SELECT * 
	FROM tabela 
	LIMIT 3 OFFSET 3




CONCATENAÇÃO:

    SELECT coluna1 || ' ' || conluna2
    FROM tabela